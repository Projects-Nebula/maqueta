# TODO

No hay tareas pendientes registradas.

El estado del proyecto se mantiene en `BACKLOG.csv`; los contratos y
escenarios vigentes están en `openspec/`. Las mejoras evaluadas del
proyecto instatic quedaron en `PROPOSAL.md` (implementadas o diferidas con
motivo documentado).