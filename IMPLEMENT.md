# IMPLEMENT.md

No hay implementación en curso.

Este archivo contiene, mientras está en curso, el trabajo activo de la
fase IMPLEMENT de la skill `delivery-loop`: qué ítem de `PLAN.md` se está
construyendo ahora, y el comando + resultado exacto de cada verificación
en vivo (servidor real, suite de tests, script `.sh` ejecutado de verdad —
nunca "el código se ve bien"). Una vez verificado y volcado a `BACKLOG.csv`
(y `learnings.jsonl` si aplica), el contenido se limpia acá.
